# Build artifacts
/target/

# Local data symlink (machine-specific; points outside the repo)
/data

# Backup / packaging leftovers
*.bak
*.orig

# Editor / OS cruft
.DS_Store
*.swp
*~

# mdBook build artifact (ADR 0083 M6)
/book
