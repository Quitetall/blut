{
  "_comment": "Checkpoint envelope v1 key schema (CONTRACT.md §4). Real envelopes are torch pickles with these keys; this file is the shape fixture the lint and both CIs check against.",
  "model": "<state_dict>",
  "opt": "<optimizer state_dict>",
  "config": {
    "_comment": "resolved run config, canonically hashable (sorted keys, normalized numbers)",
    "seq_len": 512,
    "lr": 0.0002
  },
  "step": 4200,
  "rng": {
    "python": "<random.getstate()>",
    "numpy": "<np.random.get_state()>",
    "torch": "<torch.get_rng_state()>",
    "cuda": "<torch.cuda.get_rng_state_all(), optional>"
  },
  "ema": "<optional: EMA weights state>",
  "sched": "<optional: LR-schedule state, required for stateful schedules like WSD-infinity>",
  "manifest_ref": "<optional: path or digest of the RUN_MANIFEST>"
}
